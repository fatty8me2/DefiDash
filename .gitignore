node_modules/
out/
dist/
.env
.env.local
*.log
.DS_Store
.vscode/

# Secrets — never commit real API keys
apikeys.txt

# Build artifacts
*.tsbuildinfo

# Local editor/tool state
.claude/

# Internal working notes — kept local, NOT pushed (contain operational details
# like access-gate codes and the Firebase URL).
CLAUDE.md
MEMORY.md
BACKLOG.md
RESTART.md
